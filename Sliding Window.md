Sliding window is derived from [[Two pointers]]. unlike two pointers we create a window between our pointers and also storing the result of window in a such way that we can manipulate the window while using the result of the previous result to find the current result.

# Main points

- The window needs to be maintain accordin