So when we add multiple servers we need to direct the traffic between multiple servers. For this we use a load balancer,  so like before explained in the [[Fundamentals of how applications works]] we now that how a user sends a request directly to a server's IP but when we add a load balancer user's request travel to load balancer which then direct it to the server where it should go. With this approach we usually form 